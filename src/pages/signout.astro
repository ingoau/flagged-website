---
import Layout from "../layouts/Layout.astro";
import { parseSetCookieHeader } from "better-auth/cookies";
import { auth } from "../lib/auth";
import Button from "../components/Button.astro";
import TerminalWindow from "../components/TerminalWindow.astro";

const { headers, response } = await auth.api.signOut({
    headers: Astro.request.headers,
    returnHeaders: true,
});

const setCookie = headers.get("set-cookie");
if (setCookie) {
    const cookies = parseSetCookieHeader(setCookie);

    for (const [name, attrs] of cookies) {
        Astro.cookies.set(name, attrs.value, {
            domain: attrs.domain,
            expires: attrs.expires,
            httpOnly: attrs.httponly,
            maxAge: attrs["max-age"],
            path: attrs.path ?? "/",
            sameSite: attrs.samesite,
            secure: attrs.secure,
        });
    }
}
---

<Layout>
    <div class="prose prose-xl prose-invert mx-auto p-4">
        <TerminalWindow
            titleSuffix=" --pyramid-scheme"
            bodyClass="min-h-72 flex flex-col gap-4 p-4 items-center justify-center"
        >
                Signed out
                <Button href="/">Home</Button>
        </TerminalWindow>
    </div>
</Layout>
